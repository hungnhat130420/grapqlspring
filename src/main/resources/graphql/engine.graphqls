type Query {
    customers: [Customer]
    customerById(id: ID!): Customer
      hello: String
       helloWithName(name: String): String
}

type Customer {
    id: ID
    name: String
    profile: Profile
}

type Profile {
    id: ID
    customerId: ID
}
